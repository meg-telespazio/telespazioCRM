rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      // A user can read or update their own profile
      allow read, update: if request.auth != null && request.auth.uid == userId;
      // A user can create their own profile document during sign-up
      allow create: if request.auth != null && request.resource.data.uid == userId;
    }

    match /clients/{clientId} {
      // A user can create a client if they are logged in and the createdBy field matches their uid.
      allow create: if request.auth != null && request.resource.data.createdBy == request.auth.uid;
      
      // An authenticated user can list, read, update, and delete a client if they are the owner.
      allow list, read, update, delete: if request.auth != null && resource.data.createdBy == request.auth.uid;
    }

    match /contacts/{contactId} {
      // A user can create a contact if they are logged in and the createdBy field matches their uid.
      allow create: if request.auth != null && request.resource.data.createdBy == request.auth.uid;

      // An authenticated user can list, read, update, and delete a contact if they are the owner.
      allow list, read, update, delete: if request.auth != null && resource.data.createdBy == request.auth.uid;
    }

    match /opportunities/{opportunityId} {
       // A user can create an opportunity if they are logged in and the createdBy field matches their uid.
      allow create: if request.auth != null && request.resource.data.createdBy == request.auth.uid;

      // An authenticated user can list, read, update, and delete an opportunity if they are the owner.
      allow list, read, update, delete: if request.auth != null && resource.data.createdBy == request.auth.uid;
    }
  }
}
