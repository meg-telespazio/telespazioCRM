rules_version = '2';

// Reglas de seguridad para Firebase Storage
// Fecha de actualización: 2024-05-22 (Force Redeploy)
service firebase.storage {
  match /b/{bucket}/o {
    // Permitir lectura y escritura a cualquier usuario autenticado en todo el bucket.
    // Esto es necesario para la gestión de adjuntos en Contratos y Oportunidades.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
