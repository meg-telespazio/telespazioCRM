rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Permiso total para cualquier usuario autenticado
      allow read, write: if request.auth != null;
    }
  }
}