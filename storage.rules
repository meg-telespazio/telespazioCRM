rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Permitir acceso total a cualquier usuario autenticado.
    // Esto cubre tanto la subida de adjuntos como las fotos de perfil.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}