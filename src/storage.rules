rules_version = '2';

// Reglas de seguridad para Firebase Storage (Source) - Versión Forzada 1.1
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
