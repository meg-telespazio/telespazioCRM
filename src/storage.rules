rules_version = '2';

// REGLAS PÚBLICAS TOTALES (Sincronizado) - ÚLTIMA ACTUALIZACIÓN: 1773210000000
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}